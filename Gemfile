source "https://rubygems.org"

# Matches GitHub Pages' build environment so local `jekyll serve` and the
# automatic Pages build agree.
gem "github-pages", group: :jekyll_plugins
gem "webrick" # required by Jekyll on Ruby 3+
